# Input topics handled/muxed.
# For each topic:
# - name    : name identifier to select the topic (*sub-namespace, see below)
# - topic   : input topic of geometry_msgs::Twist type
# - timeout : timeout in seconds to start discarding old messages, and use 0.0 speed instead
# - priority: priority in the range [0, 255]; the higher the more priority over other topics
/**:  #ackermann_mux:
  ros__parameters:
    topics:
      estop:
        topic   : estop  # emergency stop — highest priority, overrides all inputs
        timeout : 0.5
        priority: 255
      navigation:
        topic   : drive  # autonomous commands
        timeout : 0.2
        priority: 10
      nav_block:
        topic   : nav_block  # startup lock — blocks drive until joystick connects (see ackermann_mux.launch.py)
        timeout : 0.5
        priority: 15
      joystick:
        topic   : teleop  # human driver
        timeout : 0.1
        priority: 100
      safety:
        topic   : safety
        timeout : 0.05
        priority: 1