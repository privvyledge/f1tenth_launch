/**:
  ros__parameters:
    stateless: false
    use_mag: false
    publish_tf: false  # true
    reverse_tf: false
#    fixed_frame: "base_link"  # imu_link, base_link, odom
    # NOTE: this file is NOT loaded -- launch/filters/imu_filter.launch.py passes
    # explicit parameter dicts and its parameters=[imu_filter_param_file] line is
    # commented out. Editing values here changes nothing. constant_dt is set per
    # chain via the imu_filter_constant_dt launch argument (bug-248).
    constant_dt: 0.0
    publish_debug_topics: false
    world_frame: "enu"
    gain: 0.1
    zeta: 0.0
    mag_bias_x: 0.0
    mag_bias_y: 0.0
    mag_bias_z: 0.0
#    orientation_stddev: 0.1
    remove_gravity_vector: false
