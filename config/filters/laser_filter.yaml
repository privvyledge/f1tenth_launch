/**:
  ros__parameters:
    filter1:
      name: box_filter  # to remove parts of the car, e.g antennae from the laserscan
      type: laser_filters/LaserScanBoxFilter
      params:
        box_frame: lidar  # lidar, sensor_kit_link, base_link
        max_x: 0.246 # 0.3302 (for base_link frame)
        max_y: 0.1016
        max_z: 0.5
        min_x: -0.2 # -0.0562 (for base_link frame)
        min_y: -0.1016
        min_z: -0.5

        invert: false # activate to remove all points outside of the box

#    filter2:
#      name: box_filter  # to remove parts outside of a specified range, e.g the max laser range
#      type: laser_filters/LaserScanBoxFilter
#      params:
#        box_frame: lidar
#        max_x: 12.0
#        max_y: 12.0
#        max_z: 0.5
#        min_x: -12.0
#        min_y: -12.0
#        min_z: -0.5
#
#        invert: true # activate to remove all points outside of the box

#    filter3:
#      name: interpolation
#      type: laser_filters/InterpolationFilter